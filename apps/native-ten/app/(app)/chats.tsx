import { View, Text } from "react-native";

export default function ChatsScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Chats — TODO</Text>
    </View>
  );
}
