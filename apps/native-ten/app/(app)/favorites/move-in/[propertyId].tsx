import { View, Text } from "react-native";

export default function MoveInScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Move In — TODO</Text>
    </View>
  );
}
