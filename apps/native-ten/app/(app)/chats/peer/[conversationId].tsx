import { View, Text } from "react-native";

export default function PeerChatScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Peer Chat — TODO</Text>
    </View>
  );
}
