import { View, Text } from "react-native";

export default function HangoutScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Hangout — TODO</Text>
    </View>
  );
}
