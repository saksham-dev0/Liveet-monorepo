import { View, Text } from "react-native";

export default function ComplaintScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Complaint — TODO</Text>
    </View>
  );
}
