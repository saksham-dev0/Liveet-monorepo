import { View, Text } from "react-native";

export default function PersonalScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Personal — TODO</Text>
    </View>
  );
}
