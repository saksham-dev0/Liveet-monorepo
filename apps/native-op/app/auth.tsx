// Auth screen implementation is now routed through the
// `(auth)/index.tsx` group. This file is kept as a thin
// re-export for clarity and backwards compatibility.
export { default } from "./(auth)";

